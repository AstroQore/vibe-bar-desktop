{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the windows may ask of the core: events, window queries, and dragging the frameless mini window and popover by their bodies. Plugins used from Rust (opener, autostart, updater) need no grant here.",
  "windows": [
    "main",
    "mini",
    "popover"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-set-position",
    "core:window:allow-outer-position",
    "core:window:allow-inner-size",
    "dialog:allow-open"
  ]
}
